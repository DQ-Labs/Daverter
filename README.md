How to use this later
On a new PC: Clone the repo, create venv, download FFmpeg into bin, run build.bat.
On Linux: Pull the changes. The code is smart enough to ignore the Windows logic and still work on Linux!
